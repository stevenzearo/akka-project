akka {
  actor {
    allow-java-serialization = true
    provider = remote
    remote {
      enable-transports = [ "akka.remote.netty.tcp" ]
      netty.tcp {
        hostname = "127.0.0.1"
        port = 8400
      }
    }
  }
}
